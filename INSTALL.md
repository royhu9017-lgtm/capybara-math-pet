# 咕姆 · 安装说明

一只靠做数学题长大的卡皮巴拉。每天把作业拍给它，它逐题批改、记错题、长经验、进化形态。

---

## 安装（三步，两分钟）

### 第 1 步 · 拿到 `capybara-math-pet` 这个文件夹

解压安装包后得到它，里面应该是这样的：

```
capybara-math-pet/
├── SKILL.md
├── pet_state.json
├── rules/
│   ├── scoring.md
│   └── knowledge_points.md
├── assets/
│   └── capybara_stages.md
└── data/
```

### 第 2 步 · 放进 WorkBuddy 的 skill 目录

| 系统 | 放到哪里 |
|---|---|
| macOS / Linux | `~/.workbuddy/skills/` |
| Windows | `C:\Users\{你的用户名}\.workbuddy\skills\` |

放好后路径应该是 `~/.workbuddy/skills/capybara-math-pet/SKILL.md`。

macOS 可以用这条命令（把安装包路径换成你自己的）：

```bash
unzip ~/Downloads/capybara-math-pet-install.zip -d ~/Downloads/
cp -R ~/Downloads/capybara-math-pet ~/.workbuddy/skills/
```

Windows 用资源管理器解压后，把文件夹拖到 `C:\Users\{用户名}\.workbuddy\skills\` 即可。

### 第 3 步 · 重启一次 WorkBuddy

skill 列表是启动时读取的，**新开一个会话**才能在列表里看到它。
然后直接说「喂卡皮巴拉」就可以开始。

---

## 在安卓手机 / iPhone 上用

WorkBuddy 有官方 App（安卓在华为应用市场、小米应用商店、应用宝搜「WorkBuddy」）。
手机端有两种模式，**选哪个决定了咕姆跑在哪儿**：

| 模式 | 咕姆跑在哪 | 存档安全吗 | 适合 |
|---|---|---|---|
| **连接电脑** | 装了技能的电脑上 | 安全，存在电脑里 | **推荐**：电脑装好技能，手机当遥控器 |
| **云端工作** | 腾讯云端沙箱 | 有风险，见下 | 手边完全没有电脑时 |

### 推荐玩法：手机拍照，电脑上的咕姆批改

1. 按上面三步，先把技能装到**电脑**上（Windows 也一样能用 zip 导入）
2. 手机装 App，用**与电脑端相同的微信账号**登录（账号不一致就连不上电脑）
3. App 里切到「连接电脑」模式，拍作业照片，说「喂卡皮巴拉」
4. 批改在电脑上完成，存档照常存在电脑的 `~/.workbuddy/capybara-math-pet/`

好处：孩子拿着手机在哪儿都能打卡，咕姆的数据始终只有一份，不会两边打架。

### 手机端的两个坑（重要）

- **必须选带「视觉」标签的模型**（如 DeepSeek-V4-Flash、Kimi-K2.5），否则它看不懂作业照片
- **拍照一次只能 1 张，从相册选最多 5 张**。作业有好几页的话，先用手机相机拍完，再用「照片」一次选多张上传

### 如果孩子手边没有电脑，只能用云端

云端沙箱是否长期保留文件**没有明确保证**，咕姆的存档存在丢失风险。两个办法：

**办法一（先测再用）**：第一次在云端打完卡，隔一天再打一次，看它记不记得昨天的经验值和错题。记得住就能放心用，忘了就走办法二。

**办法二（把账本放腾讯文档）**：App 里点「+」→「腾讯文档」授权连接，新建一篇文档当「咕姆的账本」，
每次打卡后让它把存档写进去，下次打卡先读这篇文档。跨设备、家长也能直接看。

---

## 第一次用，它会问三件事

只问一次，答完就记住了：

1. **宠物叫什么？**（不答就是「咕姆」）
2. **现在在做什么数学？**（公文式第几级 / 几年级 / 哪本教辅）
3. **每天做几道题？**（不答就是 10 题）

之后每天晚上把作业拍给它，说一句「喂卡皮巴拉」就行。

---

## 重要的两件事

**一、跑满 7 天再要周报。**
不足 7 天它只会回你「还差 N 天」——不是坏了，是样本不够，弱项诊断会算出噪音。

**二、升级不会丢数据。**
打卡数据存在 `~/.workbuddy/capybara-math-pet/` 下面（不是 skill 文件夹里），
以后更新规则、加新形态，直接替换 skill 文件夹就行，经验和连续天数都还在。

---

## 常见问题

**Q：说了「喂卡皮巴拉」没反应？**
重启 WorkBuddy 再试。如果还不行，直接说「读一下 ~/.workbuddy/skills/capybara-math-pet/SKILL.md 并执行」。

**Q：两个孩子想各养一只？**
不用装两次。第二个孩子第一次用时回答一个**不同的宠物名**，数据会自动分开存，互不干扰。
（如果用的是同一台电脑或同一个云端账号，宠物名一定要不一样，否则会共用一份存档。）

**Q：想改积分规则或难度？**
改 `rules/scoring.md` 里的表就行，改完下次打卡自动生效。

**Q：批改错了怎么办？**
把那道题指出来，让它标「待确认」并回退该题分数。它的规矩是看不清一律标待确认、绝不硬猜——
如果它有题判得你不放心，多半是图上没看清，直接问它「第几题你是怎么认的」。

**Q：孩子为了宠物好看想藏错题？**
规则上已经堵住了：错题记 0 分但**不倒扣**，订正对了还能追回一半经验——
交出错题比蒙对更划算。告诉他这个规则就行。
